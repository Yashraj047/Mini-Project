<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Project</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class ="flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">Logout</a>
        </div>
        <h3 class="text-3xl font-bold mb-5">Hi <%= user.name %>😎</h3>
        <h5 class="text-lg mb-3">Write your profile details here :) </h5>
    <form action="/post" method="post">
        <textarea placeholder="Write something about anything..." class="w-1/2 px-3 py-2 outline-none rounded-md bg-transparent border-2 border-zinc-800 mb-4 resize-none" name="content"></textarea>
        <input class=" w-42 px-3 py-2 bg-blue-500 block rounded-md mt-2 text-sm" type="submit" value="Create New Post">
    </form>
    <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts...</h3>
        <div class ="postcontainer">


            <% user.posts.reverse().forEach(post => { %>
         <div class="post mb-2 w-1/3 bg-zinc-800 p-4 rounded-md mt-3 border-[1px] border-zinc-700">
                <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>
            <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small>
            <div class="btns flex gap-4 mt-5">
                <a class="text-blue-500" href="/like/<%= post._id %>">
                    <%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'Unlike' %>
                </a>
                <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
            </div>
           <% }) %>
    </div>
</body>
</html>

<!-- 
    <!DOCTYPE html>  // Yeh HTML5 ka document type hai, browser ko batata hai ki yeh HTML5 document hai.
    <html lang="en"> // Yeh HTML ka root element hai, aur 'en' language set karta hai.
    <head>           // Head section, jisme meta info, title, aur scripts hote hain.
        <meta charset="UTF-8"> // Character encoding set karta hai, taaki special characters sahi dikhe.
        <meta name="viewport" content="width=device-width, initial-scale=1.0"> // Responsive design ke liye, mobile pe sahi dikhe.
        <title>Mini Project</title> // Browser tab ka naam set karta hai.
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script> // Tailwind CSS ko CDN se load karta hai styling ke liye.
    </head>
    <body> // Body section, yahan pe saara visible content aata hai.
        <div class="w-full min-h-screen bg-zinc-900 text-white p-10"> // Full width, minimum screen height, dark background, white text, padding.
            <div class="flex justify-end"> // Flexbox use karke content ko right align karta hai.
                <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">Logout</a> // Logout button, red color, rounded, padding, margin.
            </div>
            <h3 class="text-3xl font-bold mb-5">Hi <%= user.name %>😎</h3> // User ka naam dikhata hai, bada aur bold font.
            <h5 class="text-lg mb-3">Write your profile details here :) </h5> // Chota heading, user ko prompt karta hai.
            <form action="/post" method="post"> // Form hai, jisse user post create kar sakta hai.
                <textarea placeholder="Write something about anything..." class="w-1/2 px-3 py-2 outline-none rounded-md bg-transparent border-2 border-zinc-800 mb-4 resize-none" name="content"></textarea> // Textarea jahan user apna post likh sakta hai.
                <input class=" w-42 px-3 py-2 bg-blue-500 block rounded-md mt-2 text-sm" type="submit" value="Create New Post"> // Submit button, blue color, rounded, padding.
            </form>
            <div class="posts mt-20"> // Posts section, upar se margin.
                <h3 class="text-zinc-400">Your Posts...</h3> // Posts ka heading, halka color.
                <div class="postcontainer"> // Sabhi posts ko contain karta hai.
                    <% user.posts.reverse().forEach(post => { %> // EJS ka loop, user ke posts ko reverse order me dikhata hai.
                        <div class="post mb-2 w-1/3 bg-zinc-800 p-4 rounded-md mt-3 border-[1px] border-zinc-700"> // Har post ka box, dark background, border, rounded.
                            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4> // Username dikhata hai, blue color.
                            <p class="text-sm tracking-tight"><%= post.content %></p> // Post ka content dikhata hai.
                            <div class="btns flex gap-4 mt-5"> // Buttons ke liye flexbox, gap.
                                <a class="text-blue-500" href="">Like</a> // Like button, blue color.
                                <a class="text-zinc-600" href="">Edit</a> // Edit button, grey color.
                            </div>
                        </div>
                    <% }) %> // Loop end.
                </div>
            </div>
        </div>
    </body>
</html>
-->