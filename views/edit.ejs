<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Project</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class ="flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">Logout</a>
        </div>
        <h3 class="text-3xl font-bold mb-5">Edit Your post...</h3>
        <h5 class="text-lg mb-3">Edit here</h5>
    <form action="/update/<%= post._id %>" method="post">
        <textarea placeholder="Write something about anything..." class="w-1/2 px-3 py-2 outline-none rounded-md bg-transparent border-2 border-zinc-800 mb-4 resize-none" name="content"><%= post.content %></textarea>
        <input class=" w-42 px-3 py-2 bg-yellow-500 text-black block rounded-md mt-2 text-sm" type="submit" value="Update Post">
    </form>
</body>
</html>
